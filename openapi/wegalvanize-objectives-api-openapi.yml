openapi: 3.0.2
info:
  title: HighBond API Reference Actions Objectives API
  version: 1.1.0
  description: "\nWelcome to the API documentation for HighBond.\n\n## Introduction\nThe HighBond API is the developer interface for the HighBond platform. You can use it to automate interactions with HighBond,\nbuild reporting, and integrate HighBond with other systems in your company. This introduction contains information you should\nknow before you get started.\n\nThe HighBond API is organized around <a href=\"https://en.wikipedia.org/wiki/Representational_state_transfer\" target=\"_blank\">REST</a> and is built on the\n<a href=\"https://jsonapi.org/format/\" target=\"_blank\">JSON API v1.0 specification</a>.\n\n> **Warning —**\nThis API interacts with your company's live data. It gives you the power to delete important data that your\ncompany needs. Carefully consider the consequences of your actions when using this API. If you are not sure, contact the\nperson or group who administers HighBond in your company.\n\n### Permissions\nUsers can generate and manage API tokens in their user profiles in HighBond. Because these tokens are tied to users, not companies, your permissions in the API are the same as your permissions in HighBond.\n\n> **Note —**\nYou must be a System Admin in at least one HighBond instance to access the API.\n\nIf you leave your company and your HighBond user account is deleted, your application will break. Before getting started, consider the long-term requirements of your application. You may wish to create a system account in HighBond and build your application from that, but verify that your company's security policies permit accounts that aren't tied to particular people.\n\nHighBond uses the <a href=\"https://tools.ietf.org/html/rfc6749\" target=\"_blank\">OAuth 2.0 authorization framework</a> to control access to the API.\n\n\n### Apps\nApps are components in HighBond that provide specific functionality. You can use this API to interact with data in the apps you have access to.\n\nThe apps available to you depend on the edition of HighBond (standard or premium) in use, and whether or not your company has purchased any add-on apps.\nFor more information, see <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=lp-product-editions\" target=\"_blank\">Subscriptions, editions, and capabilities</a>.\n\n### Rate limitations\nThe API safeguards against bursts of usage to help maximize its stability. For most endpoints, you cannot make more than 6 requests per second.\n\nThe following endpoints have a stricter rate limitation of 3 requests per minute:\n\n* Groups\n* Users\n\nTreat these limits as a maximum and avoid making unnecessary requests to the API.\n\nIf you make too many requests, the API responds with a `429` \"Too many requests\" status.\n\n## Getting started\nTo start interacting with your HighBond organization via the API, you need a few things:\n\n* An authentication token from your HighBond instance\n* The correct region URL for your HighBond instance\n* Information about the HighBond instance and resource IDs you want to interact with\n\nIf you need assistance with any of these, contact a HighBond administrator at your company, or email <a href=\"mailto:support@wegalvanize.com\">support@wegalvanize.com</a>.\n\n### Authentication\nAll requests require authentication. To authenticate, use Launchpad to create a HighBond API (bearer) token for your account. The token is a string that authenticates you\nand allows you to securely access the HighBond API.\n\nFor help creating a token, see <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=lp-access-tokens\" target=\"_blank\">Creating and managing access tokens</a>.\n\nOnce you have created your token, you can provide it in the header of your request:\n```\nAuthorization: Bearer <TOKEN>\n```\n > **Important —**\nIf you do not authenticate yourself, the API fails. The API responds to authentication failures with a `401` \"Bad credentials\" status.\n\n<security-definitions />\n\n### Token security\nHighBond uses the <a href=\"https://tools.ietf.org/html/rfc6749#section-1.3.1\" target=\"_blank\">Authorization Code workflow</a> to generate tokens.\n\nTo enable continuous access to the API, and to provide full ownership and accountability to users, Galvanize does not impose an expiration date\non the token. You can <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=lp-access-tokens\" target=\"_blank\">delete a token in\nLaunchpad</a> when you no longer need access to the API.\n\n> **Important —**\nYour access token provides many privileges, so keep it secure and private. Galvanize recommends that you encrypt your\nmachine to securely store the token. Avoid sharing your token in publicly accessible areas such GitHub or within client-side code.\n\n### API regions\n\nThe HighBond service is provided from different regions in order to give customers options for where your data is stored, and to enable you to\ncomply with data privacy location requirements.\n\nYour request URL must include one of the following region codes, based on where your HighBond instance resides:\n\n* **United States** — `https://apis-us.diligentoneplatform.com`\n* **Canada** — `https://apis-ca.diligentoneplatform.com`\n* **Europe** — `https://apis-eu.diligentoneplatform.com`\n* **Asia** — `https://apis-ap.diligentoneplatform.com`\n* **Australia** — `https://apis-au.diligentoneplatform.com`\n* **Africa** — `https://apis-af.diligentoneplatform.com`\n* **South America** — `https://apis-sa.diligentoneplatform.com`\n* **Japan** — `https://apis-jp.diligentoneplatform.com`\n\nAdditionally, for government clients, there are two government \"regions\":\n* **Gov** (US federal) — `https://apis.diligentoneplatform-gov.com`\n* **SLED** (state, local, and education; non-federal) — `https://apis.diligentoneplatform-gov2.com`\n\n> **Important —**\nAll API requests must be made over <a href=\"https://en.wikipedia.org/wiki/HTTPS\" target=\"_blank\">HTTPS</a>. Calls over HTTP respond with the `301` \"Moved Permanently\" status.\n\n\n### HighBond instance and resource IDs\nRequests require a HighBond instance ID and the ID of the resource you are interacting with. These IDs are numeric keys that uniquely\nidentify the HighBond instance and every resource in that instance.\n\nThe easiest way to get your HighBond instance ID is to log in to HighBond and take the ID from the URL:\n```\nhttps://accounts.highbond.com/orgs/12345\n```\n\nTo obtain IDs for resources like collections, analyses, tables, and robots, you can navigate to the item you want to reference, and take the IDs from the URL.\nFor example, this URL shows a collection ID:\n```\nhttps://example-company.results.highbond.com/projects/55843\n```\n\n> **Tip —**\nThe `GET` methods in this API also return IDs that you can use to identify resources you can act on.\n\n\n## Making requests\nOnce you have all the information you need, you can start making requests in an API client of your choice.\n\n### HTTP methods\nThe API uses the appropriate HTTP method for each action:\n* `GET` — used for retrieving resources\n* `POST` — used for creating resources\n* `PUT` / `PATCH` — used for updating resources\n* `DELETE` — used for deleting resources\n\n\n### Content type\nAlways specify the `Content-Type` of your request in the header. Supported content types are:\n* JSON API\n* JSON (for backwards compatibility)\n\n```\nContent-Type: application/vnd.api+json\nContent-Type: application/json\n```\n\nA complete **Get a table** request using `curl` would look like this:\n```\ncurl -H \"Authorization: Bearer XYZ\" -H \"Content-Type: application/vnd.api+json\" https://apis-us.highbond.com/v1/orgs/11594/tables/388930\n```\n\n### Requesting the API to return only specific fields\nFor certain resources, such as control tests, you can request the API to return only specific fields using the `fields` query parameter. Sparse fieldsets are\nhelpful for limiting your responses to only the data you require.\n\n> **Note —**\nThe `type` argument must specify the resource you want to retrieve. The value of the `fields` parameter must be a comma-delimited list that specifies\nthe name of the field(s) to be returned. Avoid whitespace before or after each comma.\n```\nfields[type]=field_name1,field_name2,fieldname3\n```\n\nA complete **Get a control test** request that only returns the `assigned_user`, `control`, and `testing_round_number` fields would look like this:\n\n```\ncurl -H \"Authorization: Bearer XYZ\" -H \"Content-Type: application/vnd.api+json\" https://apis-us.highbond.com/v1/orgs/12345/control_tests/105?fields[control_tests]=assigned_user,control,testing_round_number\n```\n\n### Requesting the API to return related resources\nTo reduce the number of requests, you may use the `include` query parameter to return related resources in the response along with the requested primary resources. Such responses are called \"compound documents\".\n\nAccepted values for the `include` parameter are documented in the `Query Parameters` section of each API method.\n\n> **Note —**\nThe `include` parameter must be a comma-delimited list that specifies\nthe name of the relationship(s) or nested relationship(s) to be returned. Avoid whitespace before or after each comma.\n\nExample - requesting a relationship and a nested relationship\n```\ninclude=control,control.objective\n```\n\nA complete **Get a control test** request that returns the `control` relationship, and `control.objective` nested relationship would look like this:\n\n```\ncurl -H \"Authorization: Bearer XYZ\" -H \"Content-Type: application/vnd.api+json\" https://apis.highbond.com/v1/orgs/12345/control_tests/105?include=control,control.objective\n```\n\n\n### Blank fields\nBlank fields are included in all response schemas as `\"\"` or `null` instead of being omitted.\n\n\n### Time zones\nAll timestamps are returned in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`.\n\nSome requests that create new data, such as creating a robot task's schedule, allow you to provide time zone information.\nFor API requests that allow for a timestamp to be specified, you must explicitly provide an ISO 8601 timestamp with time zone information.\n\n```\n\"starts_at\": \"2019-03-21T16:25:00.478Z\",\n\"starts_at_timezone\": \"America/Los_Angeles\",\n```\n\n\n### Rich text fields\n\nSome requests and responses contain rich text fields that include HTML tags. These are typically description fields. If you need to format text,\nthe best approach is to upload your raw data first and apply formatting later in the user interface.\n\n> **Note —**\nImages that exist in rich text fields are returned in responses, but they are not rendered. You cannot upload new images to rich text fields using the API.\n\n\n### User identifiers\nA unique identifier (UID) is an alphanumeric string that is associated with a user in HighBond. UIDs make it possible to address users,\nso that they can be accessed and interacted with using the API. Some requests that allow you to assign items to users, such as creating an objective,\nrequire you to specify a user's UID.\n\nTo obtain a user's UID:\n\n1. Sign in to <a href=\"https://accounts.highbond.com/login\" target=\"_blank\">HighBond</a>.\n2. If your company uses more than one instance in HighBond, make sure <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=lp-switch-instances\"\ntarget=\"_blank\">the appropriate instance is active</a>.\n3. Select **Options > Users (#)**.\n4. Navigate to the appropriate user and click the user's name.\n5. In the **User Details** side panel, copy the UID to your clipboard.\nOnce you have copied the UID, you can specify it in your request.\n\n\n### Pagination\nWhen a response consists of a large number of items, the API breaks it into separate pages for performance reasons. You can control the way the API paginates your results by specifying how many items you want to have per page, and which page you want the API to return.\n\nFor example, if you request a list of controls, the request could look like this: `{protocol}://{server}/v1/orgs/{org_id}/objectives/{objective_id}/controls?page[size]=25&page[number]=Mg==`\n\nWhere:\n\n* `page[size]=25` specifies that there should be 25 items per page. Different endpoints have different default values and maximum numbers of values you can have per page.\n* `page[number]=Mg==` specifies that the API should return the second page of results (the 26th through 50th items that match the request).\n\n > **Note —**\n In most cases, page numbers must be encoded in Base64. In this example, `Mg==` is the Base64 encoded value for `2`.\n\nAdditionally, responses include the `links` parameter, which provides URLs for the `prev` and `next` attributes (or `null` values if all of the items are on one page). Those URLs include the above parameters, so you can paste them into subsequent requests if needed.\n\n\n\n## Errors\nHighBond uses standard <a href=\"https://en.wikipedia.org/wiki/List_of_HTTP_status_codes\" target=\"_blank\">HTTP response codes</a>\nto indicate the success or failure of a request. The range of a code indicates information about why it succeeded or failed:\n\n* **`2xx` range —** Success\n* **`4xx` range —** Error given the information provided\n* **`5xx` range —** Server failed to fulfil a valid request\n\nSome common errors that occur when making requests include:\n* **`400` Bad request —** When a parameter is missing (such as the `org_id` or resource `id`), when more\nthan one unique resource is specified in the request, or when a specified data type is incompatible with the endpoint\n* **`401` Bad credentials —** When there is an authentication error\n* **`403` Forbidden —** When there is an authorization problem based on your <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=lp-managing-apps\"\ntarget=\"_blank\">role in a HighBond app</a> or <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=lp-assigning-subscriptions\" target=\"_blank\">subscription</a>\n* **`404` Not found —** When there is a problem on the client side due to an incorrect URL or resource deletion\n* **`415` Unsupported media type —** When the `Content-Type` specified is unsupported\n* **`422` Unprocessable entity —** When a required attribute is missing (such as the name of a resource), or when a conflict occurs\n(such as a script activation conflict)\n\n\n## Versioning\nAll API endpoints use a version number in the path. As can be seen in the examples, `/v1/` specifies version 1 of the API.\n\nOver time, the API will evolve. New API versions will be introduced and existing versions may be updated.\n\nSome API updates are considered a breaking change, requiring a new version of the API to be introduced. An example of a breaking\nchange is fields being changed or removed. By introducing a new version, API consumers can safely continue to use prior API versions.\n\nThe other type of API update is a non-breaking change. You should expect non-breaking change updates to existing API versions.\nAn example of a non-breaking change is adding a field to a response object. Non-breaking changes are additive. While they do\nchange the response body, they should not impact the API consumer.\n"
  x-logo:
    url: ./assets/logo.png
    altText: HighBond Logo
servers:
- url: '{protocol}://{server}/v1'
  variables:
    protocol:
      default: https
    server:
      default: apis-us.highbond.com
      enum:
      - apis-us.highbond.com
      - apis-ca.highbond.com
      - apis-eu.highbond.com
      - apis-ap.highbond.com
      - apis-au.highbond.com
      - apis-af.highbond.com
      - apis-sa.highbond.com
      - apis-jp.highbond.com
security:
- bearerToken: []
tags:
- name: Objectives
  description: 'Objectives are the basis of a project or framework. They are also the organizing containers for the work done in a project or framework.

    Each objective states the subject matter under examination and how performance will be assessed.


    Objectives may also be known as sections, processes, cycles, functional areas, application systems, risk categories, or another custom term.


    > **Note:**

    Objectives located in such resources can be accessed using the API:

    > * In projects that have an "active" state

    > * In frameworks


    <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-add-objectives" target="_blank">Learn more about objectives</a>.

    '
paths:
  /orgs/{org_id}/{parent_resource_type}/{parent_resource_id}/objectives:
    get:
      tags:
      - Objectives
      operationId: getObjectives
      summary: Get objectives
      description: Get all objectives in a project or framework.
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/parentResourceTypeParam'
      - $ref: '#/components/parameters/parentResourceIdParam'
      - $ref: '#/components/parameters/objectiveSparseFieldsetParams'
      - $ref: '#/components/parameters/pageSizeParam'
      - $ref: '#/components/parameters/pageNumberEncodedParam'
      responses:
        '200':
          description: OK.
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/GetObjectivesResponse'
        '400':
          $ref: '#/components/responses/MissingResourceId'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '415':
          $ref: '#/components/responses/NotJsonapiMediaType'
    post:
      tags:
      - Objectives
      operationId: createObjective
      summary: Create an objective
      description: 'Create a new objective in an existing project or framework.


        <strong>Limitations</strong>:

        * There can be a maximum of 500 objectives per project or framework.

        * Specifying a person''s name or email in the following fields does not automatically notify them via email: `owner`, `executive_owner`. If you want these fields to take effect, omit the `owner_user` and `executive_owner_user` fields.

        * Providing `owner_user` and `executive_owner_user` will assign the objective to the user respectively and notify them via email. In this case, the `owner` and `executive_owner` fields will be ignored.

        '
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/parentResourceTypeParam'
      - $ref: '#/components/parameters/parentResourceIdParam'
      - $ref: '#/components/parameters/objectiveSparseFieldsetParams'
      requestBody:
        description: The data required to create an objective.
        required: true
        content:
          application/vnd.api+json:
            schema:
              $ref: '#/components/schemas/CreateObjectiveRequest'
      responses:
        '201':
          description: Created.
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/GetObjectiveResponse'
        '400':
          $ref: '#/components/responses/ResourceTypeMismatch'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '415':
          $ref: '#/components/responses/NotJsonapiMediaType'
        '422':
          description: 'Unprocessable entity.

            '
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/Errors'
              example:
                errors:
                - status: 422
                  source:
                    pointer: /data/attributes/title
                  code: invalid_resource
                  detail: can't be blank
  /orgs/{org_id}/objectives/{id}:
    get:
      tags:
      - Objectives
      operationId: getObjective
      summary: Get an objective
      description: 'Get information about an objective.

        '
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/resourceIdParam'
      - $ref: '#/components/parameters/objectiveSparseFieldsetParams'
      responses:
        '200':
          description: OK.
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/GetObjectiveResponseWithIncludes'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
      - Objectives
      operationId: updateObjective
      summary: Update an objective
      description: 'Make changes to an objective in a project or framework, like renaming the objective or specifying an owner.


        <strong>Limitations</strong>:

        * You cannot update the objective if it has been checked out using

        <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-offline-client" target="_blank">Offline Projects</a> or the

        <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-mobile" target="_blank">HighBond iPad app</a>.

        * Specifying a person''s name or email in the following fields does not automatically notify them via email: `owner`, `executive_owner`. If you want these fields to take effect, omit the `owner_user` and `executive_owner_user` fields.

        * Providing `owner_user` and `executive_owner_user` will assign the objective to the user respectively and notify them via email. In this case, the `owner` and `executive_owner` fields will be ignored.

        '
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/resourceIdParam'
      - $ref: '#/components/parameters/objectiveSparseFieldsetParams'
      requestBody:
        description: The data required to update an objective.
        required: true
        content:
          application/vnd.api+json:
            schema:
              $ref: '#/components/schemas/UpdateObjectiveRequest'
      responses:
        '200':
          description: OK.
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/GetObjectiveResponseWithIncludes'
        '400':
          $ref: '#/components/responses/MissingResourceId'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '415':
          $ref: '#/components/responses/NotJsonapiMediaType'
        '422':
          description: 'Unprocessable entity.

            '
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/Errors'
              example:
                errors:
                - status: 422
                  source:
                    pointer: /data/attributes/name
                  code: invalid_resource
                  detail: can't be blank
                - status: 422
                  source:
                    pointer: /data/attributes/entities
                  code: invalid_resource
                  detail: 'Missing child or parent entity ids: [1, 2 ,3]'
                - status: 422
                  source:
                    pointer: /data/attributes/entities
                  code: invalid_resource
                  detail: 'Missing inherited entity ids: [1, 2, 3]'
                - status: 422
                  source:
                    pointer: /data/attributes/entities
                  code: invalid_resource
                  detail: Entities not found
    delete:
      tags:
      - Objectives
      operationId: deleteObjective
      summary: Delete an objective
      description: 'Permanently delete an objective from a project or framework.


        <strong>Limitations</strong>:

        You cannot delete the objective in the following scenarios:

        * if the objective is included in <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-certifications" target="_blank">an existing certification program</a>

        * if the objective has been checked out using

        <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-offline-client" target="_blank">Offline Projects</a> or the

        <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-mobile" target="_blank">HighBond iPad app</a>


        > **Caution —**

        Deleting an objective also removes associated data (including risks, controls, walkthroughs, test plans, control tests, to-dos, requests, issues, actions, time entries, etc.). Once deleted, you cannot access or restore the objective.

        '
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/resourceIdParam'
      responses:
        '204':
          $ref: '#/components/responses/NoContent'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: 'Unprocessable entity.

            '
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/Errors'
              example:
                errors:
                - status: 422
                  code: certification_association
                  detail: This objective is associated with one or more certifications. To delete this objective, you must delete the associated certification.
  /orgs/{org_id}/objectives/{objective_id}/entities:
    post:
      tags:
      - Objectives
      operationId: createObjectiveEntityLink
      summary: Create objective entity link
      description: "Link entity to an objective.\n\n> **Note**\n> * Tagging an objective to an entity automatically tags an objective with all ancestor entities and descendant entities.\n> * Request <a href=\"#operation/getObjective\">Get an objective</a> with `entities` query param to get the whole list of entities linked to an objective.\n\n<strong>Limitations</strong>:\n  * You cannot tag the objective with entity if it has been checked out using\n  <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-offline-client\" target=\"_blank\">Offline Projects</a> or the\n  <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-mobile\" target=\"_blank\">HighBond iPad app</a>.\n  * Only 1 entity can be sent in the request payload.\n\n<a href=\"https://help.highbond.com/helpdocs/auditbond/en-us/Content/projects/admin/config/Setting_up_entity_tagging.html\" target=\"_blank\">Learn more about tagging items with entities</a>.\n"
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/objectiveIdParam'
      requestBody:
        description: The data required to create a objective entity link.
        required: true
        content:
          application/vnd.api+json:
            schema:
              $ref: '#/components/schemas/CreateObjectiveEntityRequest'
      responses:
        '201':
          description: Created.
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/CreateObjectiveEntityResponse'
        '400':
          $ref: '#/components/responses/MissingResourceId'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '415':
          $ref: '#/components/responses/NotJsonapiMediaType'
        '422':
          description: 'Unprocessable entity.

            '
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/Errors'
              example:
                errors:
                - status: 422
                  source:
                    pointer: /data/attributes/base
                  code: invalid_resource
                  detail: Entity and Responsibility should reference the same account
  /orgs/{org_id}/objectives/{objective_id}/entities/{id}:
    delete:
      tags:
      - Objectives
      operationId: deleteObjectiveEntityLink
      summary: Delete objective entity link
      description: "Permanently delete objective entity link.\n\n> **Note -**\nUntagging an objective from an entity automatically untags an objective from all descendant entities.\n\n<strong>Limitations</strong>:\n  * You cannot untag an objective from an entity if it has been checked out using\n  <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-offline-client\" target=\"_blank\">Offline Projects</a> or the\n  <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-mobile\" target=\"_blank\">HighBond iPad app</a>.\n  * <a href=\"https://help.highbond.com/helpdocs/highbond/en-us/Content/projects/admin/config/Setting_up_entity_tagging.html?Highlight=Inherited\">Inherited entity tags</a> cannot be removed.\n"
      parameters:
      - $ref: '#/components/parameters/orgIdParam'
      - $ref: '#/components/parameters/objectiveIdParam'
      - $ref: '#/components/parameters/resourceIdParam'
      responses:
        '204':
          $ref: '#/components/responses/NoContent'
        '400':
          $ref: '#/components/responses/MissingOrgId'
        '401':
          $ref: '#/components/responses/BadCredentials'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '415':
          $ref: '#/components/responses/NotJsonapiMediaType'
        '422':
          description: 'Unprocessable entity.

            '
          content:
            application/vnd.api+json:
              schema:
                $ref: '#/components/schemas/Errors'
              example:
                errors:
                - status: 422
                  source:
                    pointer: /data/attributes/base
                  code: invalid_resource
                  title: Invalid resource
                  detail: Cannot delete an inherited Entity.
components:
  responses:
    NotFound:
      description: Not found.
      content:
        application/vnd.api+json:
          schema:
            $ref: '#/components/schemas/Errors'
          example:
            errors:
            - status: '404'
              title: Not Found
              detail: The requested resource does not exist.
    BadCredentials:
      description: Bad credentials.
    MissingOrgId:
      description: Bad request.
      content:
        application/vnd.api+json:
          schema:
            $ref: '#/components/schemas/Errors'
          example:
            errors:
            - status: 400
              code: parameter_missing
              title: Parameter missing
              detail: The required parameter, org_id, is missing.
    NotJsonapiMediaType:
      description: Unsupported media type.
      content:
        application/vnd.api+json:
          schema:
            $ref: '#/components/schemas/Errors'
          example:
            errors:
            - status: 415
              code: unsupported_media_type
              title: Unsupported media type
              detail: 'The endpoint supports only the application/vnd.api+json Content-Type. This request specified application/json.

                '
    Forbidden:
      description: Forbidden.
    NoContent:
      description: No content.
    MissingResourceId:
      description: Bad request.
      content:
        application/vnd.api+json:
          schema:
            $ref: '#/components/schemas/Errors'
          example:
            errors:
            - status: 400
              code: parameter_missing
              title: Parameter missing
              detail: The required parameter, id, is missing.
    ResourceTypeMismatch:
      description: Bad request.
      content:
        application/vnd.api+json:
          schema:
            $ref: '#/components/schemas/Errors'
          example:
            errors:
            - status: 400
              code: resource_type_mismatch
              title: Resource type mismatch
              detail: 'The requested data type, a-bad-type, is incompatible with the endpoint.

                '
  schemas:
    Error:
      type: object
      required:
      - status
      properties:
        status:
          type: string
          description: The HTTP status code.
        source:
          type: object
          description: Indicates which part of the request document caused the error.
          required:
          - pointer
          properties:
            pointer:
              type: string
              description: 'A JSON Pointer <a href="https://tools.ietf.org/html/rfc6901" target="_blank">(RFC6901)</a> to the associated entity

                in the request document.

                '
        code:
          type: string
          description: Application-specific error code, expressed as a string value.
        title:
          type: string
          description: A short, human-readable summary of the problem.
        detail:
          type: string
          description: A human-readable explanation specific to this occurrence of the problem.
    UpdateObjectiveRequest:
      type: object
      description: The data associated with the resource.
      required:
      - data
      properties:
        data:
          type: object
          description: The data associated with the resource.
          required:
          - id
          - type
          - attributes
          properties:
            id:
              type: string
              description: The ID of the requested resource.
              example: '1'
            type:
              type: string
              description: The classification of the object (`"objectives"`).
              example: objectives
            attributes:
              type: object
              description: The attributes associated with the resource.
              required:
              - title
              - reference
              properties:
                title:
                  type: string
                  description: The title of the objective.
                  maxLength: 60
                  example: Objective Title
                reference:
                  type: string
                  description: The reference number or code for the objective.
                  maxLength: 8
                  example: ITC
                description:
                  type: string
                  description: A detailed description of the objective.
                  example: Objective description
                division_department:
                  type: string
                  description: The division or department responsible for the objective.
                  example: Finance
                owner:
                  type: string
                  description: 'The person responsible for the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `owner_user` field of the `relationships` section in the request data.

                    '
                  example: thomas@sodor.ca
                executive_owner:
                  type: string
                  description: 'The executive owner of the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `executive_owner_user` field of the `relationships` section in the request data.

                    '
                  example: henry@sodor.com
                position:
                  type: integer
                  format: int64
                  description: 'The value that determines the order in which it is displayed in the index action. `position` must be a value between `1` and `2147483647`.

                    '
                  example: 1
                custom_attributes:
                  type: array
                  description: An array of custom attributes defined on the project type used by the project this objective is from. <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-project-types" target="_blank">Learn more about project types</a>. Make a PATCH request to modify the value of a custom attribute.
                  items:
                    type: object
                    description: A user defined custom attribute value.
                    properties:
                      id:
                        type: string
                        description: The ID of this custom attribute. Optional if term is included.
                        example: '42'
                      term:
                        type: string
                        description: The user defined term used to identify this custom attribute. Optional if id is included.
                        example: Custom Objective Attribute
                      value:
                        type: array
                        description: The value or values (for multiselect) for this custom attribute.
                        example:
                        - Custom Select Option Text
                        items:
                          type: string
                planned_start_date:
                  type: string
                  nullable: true
                  description: Planned start date
                  example: '2019-02-09'
                  format: date
                planned_end_date:
                  type: string
                  nullable: true
                  description: Planned end date.
                  example: '2019-02-10'
                  format: date
                actual_start_date:
                  type: string
                  nullable: true
                  description: Actual start date.
                  example: '2019-02-09'
                  format: date
                actual_end_date:
                  type: string
                  nullable: true
                  description: Actual end date.
                  example: '2019-02-10'
                  format: date
                actual_milestone_date:
                  type: string
                  nullable: true
                  description: Actual milestone date.
                  example: '2019-02-10'
                  format: date
                planned_milestone_date:
                  type: string
                  nullable: true
                  description: Planned milestone date.
                  example: '2019-02-09'
                  format: date
            relationships:
              type: object
              description: The relationships associated with the resource.
              properties:
                assigned_user:
                  type: object
                  description: 'The person assigned to the objective. Only users assigned the Professional Manager or Professional User role in the project can be specified.

                    The user specified receives an email notification with a link to the objective, is able to view the objective on the project dashboard,

                    and has full read and write access to the objective.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                entities:
                  type: object
                  description: 'The passed entities list should match the following conditions:

                    1. It should be the full exact list of entities you would like to be linked to the resource.


                    2. The passed entities should consist of valid sub-trees with all ancestor entities as well as descendent entities.


                    3. Inherited entities should be included too.


                    Otherwise, the whole request will be rejected with the corresponding error.

                    '
                  properties:
                    data:
                      type: array
                      items:
                        type: object
                        properties:
                          id:
                            type: string
                            description: The ID of the entity.
                            example: '1'
                          type:
                            type: string
                            description: The classification of the object (`"entities"`).
                            example: entities
                framework_origin:
                  type: object
                  description: 'In the Projects app, a project `objective` can have a related counterpart in an associated framework. The `framework_origin` attribute allows you to associate an `objective` in a project with its related counterpart in a framework. Can be set only when `parent_resource_type` is `projects`.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the parent objective.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"objectives"`).
                          example: objectives
                owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is the key person accountable for the process/risk/control (often the manager of the function).

                    Managers responsible for an entire objective that have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an owner of an objective.

                    Once a person is assigned as an owner of an objective, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                executive_owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an executive owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is a sponsor/leader accountable for the process/risk/control (often the VP/C-Suite of the owner).

                    This may be board or executive members that are responsible for an entire objective and have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an executive owner of an objective.

                    After a person has been assigned as executive owner, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `executive_owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
    Errors:
      type: object
      properties:
        errors:
          type: array
          items:
            $ref: '#/components/schemas/Error'
    CreateObjectiveEntityRequest:
      type: object
      description: The data associated with the resource.
      required:
      - data
      properties:
        data:
          type: object
          description: The data associated with the resource.
          required:
          - id
          - type
          properties:
            id:
              type: string
              description: The ID of the objective.
              example: '1'
            type:
              type: string
              description: The classification of the object (`"entities"`).
              example: entities
    GetObjectiveResponseWithIncludes:
      type: object
      description: The data included in the response.
      required:
      - data
      properties:
        data:
          type: object
          description: The data associated with the resource.
          required:
          - id
          - type
          - attributes
          - relationships
          properties:
            id:
              type: string
              description: The ID of the requested resource.
              example: '1'
            type:
              type: string
              description: The classification of the object (`"objectives"`).
              example: objectives
            attributes:
              type: object
              description: The attributes associated with the resource.
              required:
              - title
              - reference
              properties:
                title:
                  type: string
                  description: The title of the objective.
                  maxLength: 60
                  example: Objective Title
                reference:
                  type: string
                  description: The reference number or code for the objective.
                  maxLength: 8
                  example: ITC
                description:
                  type: string
                  description: A detailed description of the objective.
                  example: Objective description
                division_department:
                  type: string
                  description: The division or department responsible for the objective.
                  example: Finance
                owner:
                  type: string
                  description: 'The person responsible for the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `owner_user` field of the `relationships` section in the request data.

                    '
                  example: thomas@sodor.ca
                executive_owner:
                  type: string
                  description: 'The executive owner of the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `executive_owner_user` field of the `relationships` section in the request data.

                    '
                  example: henry@sodor.com
                position:
                  type: integer
                  format: int64
                  description: 'The value that determines the order in which it is displayed in the index action. `position` must be a value between `1` and `2147483647`.

                    '
                  example: 1
                risk_control_matrix_id:
                  type: string
                  description: The id of the risk control matrix.
                  example: '100'
                walkthrough_summary_id:
                  type: string
                  description: The id of the walkthrough summary.
                  nullable: true
                  example: '200'
                testing_round_1_id:
                  type: string
                  description: The id of testing round 1 (if applicable).
                  nullable: true
                  example: '301'
                testing_round_2_id:
                  type: string
                  description: The id of testing round 2 (if applicable).
                  nullable: true
                  example: '302'
                testing_round_3_id:
                  type: string
                  description: The id of testing round 3 (if applicable).
                  nullable: true
                  example: '303'
                testing_round_4_id:
                  type: string
                  description: The id of testing round 4 (if applicable).
                  nullable: true
                  example: '304'
                created_at:
                  type: string
                  description: The timestamp identifying when the objective was created.
                  example: '2019-02-09T13:17:26Z'
                  format: date-time
                updated_at:
                  type: string
                  description: The timestamp identifying when the objective was last updated.
                  example: '2019-02-09T13:17:26Z'
                  format: date-time
                planned_start_date:
                  type: string
                  nullable: true
                  description: Planned start date
                  example: '2019-02-09'
                  format: date
                planned_end_date:
                  type: string
                  nullable: true
                  description: Planned end date.
                  example: '2019-02-10'
                  format: date
                actual_start_date:
                  type: string
                  nullable: true
                  description: Actual start date.
                  example: '2019-02-09'
                  format: date
                actual_end_date:
                  type: string
                  nullable: true
                  description: Actual end date.
                  example: '2019-02-10'
                  format: date
                actual_milestone_date:
                  type: string
                  nullable: true
                  description: Actual milestone date.
                  example: '2019-02-10'
                  format: date
                planned_milestone_date:
                  type: string
                  nullable: true
                  description: Planned milestone date.
                  example: '2019-02-09'
                  format: date
                custom_attributes:
                  type: array
                  description: An array of custom attributes defined on the project type used by the project this objective is from. <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-project-types" target="_blank">Learn more about project types</a>. Make a PATCH request to modify the value of a custom attribute.
                  items:
                    type: object
                    description: A user defined custom attribute value.
                    properties:
                      id:
                        type: string
                        description: The ID of this custom attribute. Optional if term is included.
                        example: '42'
                      term:
                        type: string
                        description: The user defined term used to identify this custom attribute. Optional if id is included.
                        example: Custom Objective Attribute
                      value:
                        type: array
                        description: The value or values (for multiselect) for this custom attribute.
                        example:
                        - Custom Select Option Text
                        items:
                          type: string
                risk_assurance_data:
                  type: object
                  description: 'Fields that determines assurance values. Assurance is an expression of confidence that risk is effectively mitigated. Returns `null` if Risk Assurance feature is disabled.

                    '
                  properties:
                    inherent_risk:
                      type: number
                      format: float
                      description: 'The risk that an activity would pose if no controls or other mitigating factors were in place. Can also be referred to as the gross risk, or risks before controls, and can act as the baseline for your organization''s risk tolerance or appetite.

                        '
                      example: 1.1
                    residual_risk:
                      type: number
                      format: float
                      description: 'The risk that remains after controls are taken into account. Can also be referred to as the net risk or risk after controls.

                        '
                      example: 1.1
                    assurance:
                      type: number
                      description: 'The level of assurance that the controls in place effectively mitigate risk to the business.

                        '
                      example: 0
            relationships:
              type: object
              description: The relationships associated with the resource.
              required:
              - assigned_user
              properties:
                project:
                  type: object
                  description: 'Each objective has an associated project or framework. Project/framework is a management system that allows you to define and compile information

                    for reporting purposes.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the project.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"projects"`).
                          example: projects
                framework:
                  type: object
                  description: 'Each objective has an associated project or framework. Project/framework is a management system that allows you to define and compile information

                    for reporting purposes.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the framework.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"frameworks"`).
                          example: frameworks
                framework_origin:
                  type: object
                  description: 'In the Projects app, a project `objective` can have a related counterpart in an associated framework. The `framework_origin` attribute allows you to associate an `objective` in a project with its related counterpart in a framework. Can be set only when `parent_resource_type` is `projects`.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the parent objective.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"objectives"`).
                          example: objectives
                assigned_user:
                  type: object
                  description: 'The person assigned to the objective. Only users assigned the Professional Manager or Professional User role in the project can be specified.

                    The user specified receives an email notification with a link to the objective, is able to view the objective on the project dashboard,

                    and has full read and write access to the objective.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                entities:
                  type: object
                  description: Each objective has an associated objective entities.
                  required:
                  - data
                  properties:
                    data:
                      type: array
                      description: The data associated with the resource.
                      items:
                        type: object
                        required:
                        - id
                        - type
                        properties:
                          id:
                            type: string
                            description: The ID of the entity.
                            example: '1'
                          type:
                            type: string
                            description: The classification of the object (`"entities"`).
                            example: entities
                owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is the key person accountable for the process/risk/control (often the manager of the function).

                    Managers responsible for an entire objective that have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an owner of an objective.

                    Once a person is assigned as an owner of an objective, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                executive_owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an executive owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is a sponsor/leader accountable for the process/risk/control (often the VP/C-Suite of the owner).

                    This may be board or executive members that are responsible for an entire objective and have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an executive owner of an objective.

                    After a person has been assigned as executive owner, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `executive_owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
    CreateObjectiveRequest:
      type: object
      description: The data associated with the resource.
      required:
      - data
      properties:
        data:
          type: object
          description: The data associated with the resource.
          required:
          - type
          - attributes
          - relationships
          properties:
            type:
              type: string
              description: The classification of the object (`"objectives"`).
              example: objectives
            attributes:
              type: object
              description: The attributes associated with the resource.
              required:
              - title
              - reference
              properties:
                title:
                  type: string
                  description: The title of the objective.
                  maxLength: 60
                  example: Objective Title
                reference:
                  type: string
                  description: The reference number or code for the objective.
                  maxLength: 8
                  example: ITC
                description:
                  type: string
                  description: A detailed description of the objective.
                  example: Objective description
                division_department:
                  type: string
                  description: The division or department responsible for the objective.
                  example: Finance
                owner:
                  type: string
                  description: 'The person responsible for the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `owner_user` field of the `relationships` section in the request data.

                    '
                  example: thomas@sodor.ca
                executive_owner:
                  type: string
                  description: 'The executive owner of the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `executive_owner_user` field of the `relationships` section in the request data.

                    '
                  example: henry@sodor.com
                position:
                  type: integer
                  format: int64
                  description: 'The value that determines the order in which it is displayed in the index action. `position` must be a value between `1` and `2147483647`.

                    '
                  example: 1
                custom_attributes:
                  type: array
                  description: An array of custom attributes defined on the project type used by the project this objective is from. <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-project-types" target="_blank">Learn more about project types</a>. Make a PATCH request to modify the value of a custom attribute.
                  items:
                    type: object
                    description: A user defined custom attribute value.
                    properties:
                      id:
                        type: string
                        description: The ID of this custom attribute. Optional if term is included.
                        example: '42'
                      term:
                        type: string
                        description: The user defined term used to identify this custom attribute. Optional if id is included.
                        example: Custom Objective Attribute
                      value:
                        type: array
                        description: The value or values (for multiselect) for this custom attribute.
                        example:
                        - Custom Select Option Text
                        items:
                          type: string
            relationships:
              type: object
              description: The relationships associated with the resource.
              required:
              - assigned_user
              properties:
                assigned_user:
                  type: object
                  description: 'The person assigned to the objective. Only users assigned the Professional Manager or Professional User role in the project can be specified.

                    The user specified receives an email notification with a link to the objective, is able to view the objective on the project dashboard,

                    and has full read and write access to the objective.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                framework_origin:
                  type: object
                  description: 'In the Projects app, a project `objective` can have a related counterpart in an associated framework. The `framework_origin` attribute allows you to associate an `objective` in a project with its related counterpart in a framework. Can be set only when `parent_resource_type` is `projects`.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the parent objective.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"objectives"`).
                          example: objectives
                owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is the key person accountable for the process/risk/control (often the manager of the function).

                    Managers responsible for an entire objective that have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an owner of an objective.

                    Once a person is assigned as an owner of an objective, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                executive_owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an executive owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is a sponsor/leader accountable for the process/risk/control (often the VP/C-Suite of the owner).

                    This may be board or executive members that are responsible for an entire objective and have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an executive owner of an objective.

                    After a person has been assigned as executive owner, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `executive_owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
    GetObjectivesResponse:
      type: object
      description: The data associated with the resource.
      required:
      - data
      - links
      properties:
        data:
          type: array
          items:
            type: object
            description: The data associated with the resource.
            required:
            - id
            - type
            - attributes
            - relationships
            properties:
              id:
                type: string
                description: The ID of the requested resource.
                example: '1'
              type:
                type: string
                description: The classification of the object (`"objectives"`).
                example: objectives
              attributes:
                type: object
                description: The attributes associated with the resource.
                required:
                - title
                - reference
                properties:
                  title:
                    type: string
                    description: The title of the objective.
                    maxLength: 60
                    example: Objective Title
                  reference:
                    type: string
                    description: The reference number or code for the objective.
                    maxLength: 8
                    example: ITC
                  description:
                    type: string
                    description: A detailed description of the objective.
                    example: Objective description
                  division_department:
                    type: string
                    description: The division or department responsible for the objective.
                    example: Finance
                  owner:
                    type: string
                    description: 'The person responsible for the objective. Specifying a person''s name or email does not automatically send them

                      an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                      omit the `owner_user` field of the `relationships` section in the request data.

                      '
                    example: thomas@sodor.ca
                  executive_owner:
                    type: string
                    description: 'The executive owner of the objective. Specifying a person''s name or email does not automatically send them

                      an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                      omit the `executive_owner_user` field of the `relationships` section in the request data.

                      '
                    example: henry@sodor.com
                  position:
                    type: integer
                    format: int64
                    description: 'The value that determines the order in which it is displayed in the index action. `position` must be a value between `1` and `2147483647`.

                      '
                    example: 1
                  risk_control_matrix_id:
                    type: string
                    description: The id of the risk control matrix.
                    example: '100'
                  walkthrough_summary_id:
                    type: string
                    description: The id of the walkthrough summary.
                    nullable: true
                    example: '200'
                  testing_round_1_id:
                    type: string
                    description: The id of testing round 1 (if applicable).
                    nullable: true
                    example: '301'
                  testing_round_2_id:
                    type: string
                    description: The id of testing round 2 (if applicable).
                    nullable: true
                    example: '302'
                  testing_round_3_id:
                    type: string
                    description: The id of testing round 3 (if applicable).
                    nullable: true
                    example: '303'
                  testing_round_4_id:
                    type: string
                    description: The id of testing round 4 (if applicable).
                    nullable: true
                    example: '304'
                  created_at:
                    type: string
                    description: The timestamp identifying when the objective was created.
                    example: '2019-02-09T13:17:26Z'
                    format: date-time
                  updated_at:
                    type: string
                    description: The timestamp identifying when the objective was last updated.
                    example: '2019-02-09T13:17:26Z'
                    format: date-time
                  planned_start_date:
                    type: string
                    nullable: true
                    description: Planned start date
                    example: '2019-02-09'
                    format: date
                  planned_end_date:
                    type: string
                    nullable: true
                    description: Planned end date.
                    example: '2019-02-10'
                    format: date
                  actual_start_date:
                    type: string
                    nullable: true
                    description: Actual start date.
                    example: '2019-02-09'
                    format: date
                  actual_end_date:
                    type: string
                    nullable: true
                    description: Actual end date.
                    example: '2019-02-10'
                    format: date
                  actual_milestone_date:
                    type: string
                    nullable: true
                    description: Actual milestone date.
                    example: '2019-02-10'
                    format: date
                  planned_milestone_date:
                    type: string
                    nullable: true
                    description: Planned milestone date.
                    example: '2019-02-09'
                    format: date
                  custom_attributes:
                    type: array
                    description: An array of custom attributes defined on the project type used by the project this objective is from. <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-project-types" target="_blank">Learn more about project types</a>. Make a PATCH request to modify the value of a custom attribute.
                    items:
                      type: object
                      description: A user defined custom attribute value.
                      properties:
                        id:
                          type: string
                          description: The ID of this custom attribute. Optional if term is included.
                          example: '42'
                        term:
                          type: string
                          description: The user defined term used to identify this custom attribute. Optional if id is included.
                          example: Custom Objective Attribute
                        value:
                          type: array
                          description: The value or values (for multiselect) for this custom attribute.
                          example:
                          - Custom Select Option Text
                          items:
                            type: string
                  risk_assurance_data:
                    type: object
                    description: 'Fields that determines assurance values. Assurance is an expression of confidence that risk is effectively mitigated. Returns `null` if Risk Assurance feature is disabled.

                      '
                    properties:
                      inherent_risk:
                        type: number
                        format: float
                        description: 'The risk that an activity would pose if no controls or other mitigating factors were in place. Can also be referred to as the gross risk, or risks before controls, and can act as the baseline for your organization''s risk tolerance or appetite.

                          '
                        example: 1.1
                      residual_risk:
                        type: number
                        format: float
                        description: 'The risk that remains after controls are taken into account. Can also be referred to as the net risk or risk after controls.

                          '
                        example: 1.1
                      assurance:
                        type: number
                        description: 'The level of assurance that the controls in place effectively mitigate risk to the business.

                          '
                        example: 0
              relationships:
                type: object
                description: The relationships associated with the resource.
                required:
                - assigned_user
                properties:
                  project:
                    type: object
                    description: 'Each objective has an associated project or framework. Project/framework is a management system that allows you to define and compile information

                      for reporting purposes.

                      '
                    required:
                    - data
                    properties:
                      data:
                        type: object
                        description: The data associated with the resource.
                        required:
                        - id
                        - type
                        properties:
                          id:
                            type: string
                            description: The ID of the project.
                            example: '1'
                          type:
                            type: string
                            description: The classification of the object (`"projects"`).
                            example: projects
                  framework:
                    type: object
                    description: 'Each objective has an associated project or framework. Project/framework is a management system that allows you to define and compile information

                      for reporting purposes.

                      '
                    required:
                    - data
                    properties:
                      data:
                        type: object
                        description: The data associated with the resource.
                        required:
                        - id
                        - type
                        properties:
                          id:
                            type: string
                            description: The ID of the framework.
                            example: '1'
                          type:
                            type: string
                            description: The classification of the object (`"frameworks"`).
                            example: frameworks
                  framework_origin:
                    type: object
                    description: 'In the Projects app, a project `objective` can have a related counterpart in an associated framework. The `framework_origin` attribute allows you to associate an `objective` in a project with its related counterpart in a framework. Can be set only when `parent_resource_type` is `projects`.

                      '
                    required:
                    - data
                    properties:
                      data:
                        type: object
                        description: The data associated with the resource.
                        required:
                        - id
                        - type
                        properties:
                          id:
                            type: string
                            description: The ID of the parent objective.
                            example: '1'
                          type:
                            type: string
                            description: The classification of the object (`"objectives"`).
                            example: objectives
                  assigned_user:
                    type: object
                    description: 'The person assigned to the objective. Only users assigned the Professional Manager or Professional User role in the project can be specified.

                      The user specified receives an email notification with a link to the objective, is able to view the objective on the project dashboard,

                      and has full read and write access to the objective.

                      '
                    required:
                    - data
                    properties:
                      data:
                        type: object
                        description: The data associated with the resource.
                        required:
                        - id
                        - type
                        properties:
                          id:
                            type: string
                            description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                              <a href="#section/Making-requests">Making requests</a>.

                              '
                            example: 3NQ6XzAUxqJMnAQ7n4KF
                          type:
                            type: string
                            description: The classification of the object (`"users"`).
                            example: users
                  entities:
                    type: object
                    description: Each objective has an associated objective entities.
                    required:
                    - data
                    properties:
                      data:
                        type: array
                        description: The data associated with the resource.
                        items:
                          type: object
                          required:
                          - id
                          - type
                          properties:
                            id:
                              type: string
                              description: The ID of the entity.
                              example: '1'
                            type:
                              type: string
                              description: The classification of the object (`"entities"`).
                              example: entities
                  owner_user:
                    type: object
                    description: 'Allows you to assign a licensed or non-licensed user as an owner of the objective for tracking and reporting purposes. This is an optional field.

                      Typically this is the key person accountable for the process/risk/control (often the manager of the function).

                      Managers responsible for an entire objective that have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an owner of an objective.

                      Once a person is assigned as an owner of an objective, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                      If you provide this field when creating or updating an objective, the `owner` field will be ignored.

                      '
                    required:
                    - data
                    properties:
                      data:
                        type: object
                        description: The data associated with the resource.
                        properties:
                          id:
                            type: string
                            description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                              <a href="#section/Making-requests">Making requests</a>.

                              '
                            example: 3NQ6XzAUxqJMnAQ7n4KF
                          type:
                            type: string
                            description: The classification of the object (`"users"`).
                            example: users
                  executive_owner_user:
                    type: object
                    description: 'Allows you to assign a licensed or non-licensed user as an executive owner of the objective for tracking and reporting purposes. This is an optional field.

                      Typically this is a sponsor/leader accountable for the process/risk/control (often the VP/C-Suite of the owner).

                      This may be board or executive members that are responsible for an entire objective and have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an executive owner of an objective.

                      After a person has been assigned as executive owner, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                      If you provide this field when creating or updating an objective, the `executive_owner` field will be ignored.

                      '
                    required:
                    - data
                    properties:
                      data:
                        type: object
                        description: The data associated with the resource.
                        properties:
                          id:
                            type: string
                            description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                              <a href="#section/Making-requests">Making requests</a>.

                              '
                            example: 3NQ6XzAUxqJMnAQ7n4KF
                          type:
                            type: string
                            description: The classification of the object (`"users"`).
                            example: users
        links:
          type: object
          required:
          - next
          - prev
          properties:
            next:
              type: string
              description: Next page url
              nullable: true
              example: /api/resource?page[number]=Mg==
            prev:
              type: string
              description: Previous page url
              nullable: true
              example: null
    GetObjectiveResponse:
      type: object
      description: The data associated with the resource.
      required:
      - data
      - links
      properties:
        data:
          type: object
          description: The data associated with the resource.
          required:
          - id
          - type
          - attributes
          - relationships
          properties:
            id:
              type: string
              description: The ID of the requested resource.
              example: '1'
            type:
              type: string
              description: The classification of the object (`"objectives"`).
              example: objectives
            attributes:
              type: object
              description: The attributes associated with the resource.
              required:
              - title
              - reference
              properties:
                title:
                  type: string
                  description: The title of the objective.
                  maxLength: 60
                  example: Objective Title
                reference:
                  type: string
                  description: The reference number or code for the objective.
                  maxLength: 8
                  example: ITC
                description:
                  type: string
                  description: A detailed description of the objective.
                  example: Objective description
                division_department:
                  type: string
                  description: The division or department responsible for the objective.
                  example: Finance
                owner:
                  type: string
                  description: 'The person responsible for the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `owner_user` field of the `relationships` section in the request data.

                    '
                  example: thomas@sodor.ca
                executive_owner:
                  type: string
                  description: 'The executive owner of the objective. Specifying a person''s name or email does not automatically send them

                    an email notification about the objective. If you want this field to take effect when creating or updating an objective,

                    omit the `executive_owner_user` field of the `relationships` section in the request data.

                    '
                  example: henry@sodor.com
                position:
                  type: integer
                  format: int64
                  description: 'The value that determines the order in which it is displayed in the index action. `position` must be a value between `1` and `2147483647`.

                    '
                  example: 1
                risk_control_matrix_id:
                  type: string
                  description: The id of the risk control matrix.
                  example: '100'
                walkthrough_summary_id:
                  type: string
                  description: The id of the walkthrough summary.
                  nullable: true
                  example: '200'
                testing_round_1_id:
                  type: string
                  description: The id of testing round 1 (if applicable).
                  nullable: true
                  example: '301'
                testing_round_2_id:
                  type: string
                  description: The id of testing round 2 (if applicable).
                  nullable: true
                  example: '302'
                testing_round_3_id:
                  type: string
                  description: The id of testing round 3 (if applicable).
                  nullable: true
                  example: '303'
                testing_round_4_id:
                  type: string
                  description: The id of testing round 4 (if applicable).
                  nullable: true
                  example: '304'
                created_at:
                  type: string
                  description: The timestamp identifying when the objective was created.
                  example: '2019-02-09T13:17:26Z'
                  format: date-time
                updated_at:
                  type: string
                  description: The timestamp identifying when the objective was last updated.
                  example: '2019-02-09T13:17:26Z'
                  format: date-time
                planned_start_date:
                  type: string
                  nullable: true
                  description: Planned start date
                  example: '2019-02-09'
                  format: date
                planned_end_date:
                  type: string
                  nullable: true
                  description: Planned end date.
                  example: '2019-02-10'
                  format: date
                actual_start_date:
                  type: string
                  nullable: true
                  description: Actual start date.
                  example: '2019-02-09'
                  format: date
                actual_end_date:
                  type: string
                  nullable: true
                  description: Actual end date.
                  example: '2019-02-10'
                  format: date
                actual_milestone_date:
                  type: string
                  nullable: true
                  description: Actual milestone date.
                  example: '2019-02-10'
                  format: date
                planned_milestone_date:
                  type: string
                  nullable: true
                  description: Planned milestone date.
                  example: '2019-02-09'
                  format: date
                custom_attributes:
                  type: array
                  description: An array of custom attributes defined on the project type used by the project this objective is from. <a href="https://help.highbond.com/helpdocs/highbond/en-us/Default.htm#cshid=pm-project-types" target="_blank">Learn more about project types</a>. Make a PATCH request to modify the value of a custom attribute.
                  items:
                    type: object
                    description: A user defined custom attribute value.
                    properties:
                      id:
                        type: string
                        description: The ID of this custom attribute. Optional if term is included.
                        example: '42'
                      term:
                        type: string
                        description: The user defined term used to identify this custom attribute. Optional if id is included.
                        example: Custom Objective Attribute
                      value:
                        type: array
                        description: The value or values (for multiselect) for this custom attribute.
                        example:
                        - Custom Select Option Text
                        items:
                          type: string
                risk_assurance_data:
                  type: object
                  description: 'Fields that determines assurance values. Assurance is an expression of confidence that risk is effectively mitigated. Returns `null` if Risk Assurance feature is disabled.

                    '
                  properties:
                    inherent_risk:
                      type: number
                      format: float
                      description: 'The risk that an activity would pose if no controls or other mitigating factors were in place. Can also be referred to as the gross risk, or risks before controls, and can act as the baseline for your organization''s risk tolerance or appetite.

                        '
                      example: 1.1
                    residual_risk:
                      type: number
                      format: float
                      description: 'The risk that remains after controls are taken into account. Can also be referred to as the net risk or risk after controls.

                        '
                      example: 1.1
                    assurance:
                      type: number
                      description: 'The level of assurance that the controls in place effectively mitigate risk to the business.

                        '
                      example: 0
            relationships:
              type: object
              description: The relationships associated with the resource.
              required:
              - assigned_user
              properties:
                project:
                  type: object
                  description: 'Each objective has an associated project or framework. Project/framework is a management system that allows you to define and compile information

                    for reporting purposes.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the project.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"projects"`).
                          example: projects
                framework:
                  type: object
                  description: 'Each objective has an associated project or framework. Project/framework is a management system that allows you to define and compile information

                    for reporting purposes.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the framework.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"frameworks"`).
                          example: frameworks
                framework_origin:
                  type: object
                  description: 'In the Projects app, a project `objective` can have a related counterpart in an associated framework. The `framework_origin` attribute allows you to associate an `objective` in a project with its related counterpart in a framework. Can be set only when `parent_resource_type` is `projects`.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: The ID of the parent objective.
                          example: '1'
                        type:
                          type: string
                          description: The classification of the object (`"objectives"`).
                          example: objectives
                assigned_user:
                  type: object
                  description: 'The person assigned to the objective. Only users assigned the Professional Manager or Professional User role in the project can be specified.

                    The user specified receives an email notification with a link to the objective, is able to view the objective on the project dashboard,

                    and has full read and write access to the objective.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      required:
                      - id
                      - type
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                entities:
                  type: object
                  description: Each objective has an associated objective entities.
                  required:
                  - data
                  properties:
                    data:
                      type: array
                      example: []
                      description: The data associated with the resource.
                      items:
                        type: object
                        required:
                        - id
                        - type
                        properties:
                          id:
                            type: string
                            description: The ID of the entity.
                            example: '1'
                          type:
                            type: string
                            description: The classification of the object (`"entities"`).
                            example: entities
                owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is the key person accountable for the process/risk/control (often the manager of the function).

                    Managers responsible for an entire objective that have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an owner of an objective.

                    Once a person is assigned as an owner of an objective, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
                executive_owner_user:
                  type: object
                  description: 'Allows you to assign a licensed or non-licensed user as an executive owner of the objective for tracking and reporting purposes. This is an optional field.

                    Typically this is a sponsor/leader accountable for the process/risk/control (often the VP/C-Suite of the owner).

                    This may be board or executive members that are responsible for an entire objective and have been assigned the Contributor Tester or Contributor User or Contributor Manager role are assigned as an executive owner of an objective.

                    After a person has been assigned as executive owner, they receive an email notification with a link to the objective, granting them write access to controls and read access to risks and the objective overview.

                    If you provide this field when creating or updating an objective, the `executive_owner` field will be ignored.

                    '
                  required:
                  - data
                  properties:
                    data:
                      type: object
                      description: The data associated with the resource.
                      properties:
                        id:
                          type: string
                          description: 'The unique identifier (UID) of the assigned user. For more information on obtaining this value, see

                            <a href="#section/Making-requests">Making requests</a>.

                            '
                          example: 3NQ6XzAUxqJMnAQ7n4KF
                        type:
                          type: string
                          description: The classification of the object (`"users"`).
                          example: users
    CreateObjectiveEntityResponse:
      type: object
      required:
      - data
      properties:
        data:
          type: object
          description: The data associated with the resource.
          required:
          - id
          - type
          properties:
            id:
              type: string
              description: The ID of the objective.
              example: '1'
            type:
              type: string
              description: The classification of the object (`"entities"`).
              example: entities
  parameters:
    orgIdParam:
      in: path
      name: org_id
      description: The ID of the HighBond instance.
      required: true
      schema:
        type: string
        example: 1
    parentResourceTypeParam:
      in: path
      name: parent_resource_type
      description: The type of the parent resource.
      required: true
      schema:
        type: string
        enum:
        - projects
        - frameworks
        default: projects
    resourceIdParam:
      in: path
      name: id
      description: The ID of the requested resource.
      required: true
      schema:
        type: string
        example: 9999
    objectiveIdParam:
      in: path
      name: objective_id
      description: The ID of the objective.
      required: true
      schema:
        type: integer
        example: 1
    pageSizeParam:
      in: query
      name: page[size]
      description: The number of items returned per page. Default is 50. Maximum is 100.
      required: false
      schema:
        type: string
        example: 25
    pageNumberEncodedParam:
      in: query
      name: page[number]
      description: The Base64-encoded page number of records to return.
      required: false
      schema:
        type: string
        example: Mg==
    parentResourceIdParam:
      in: path
      name: parent_resource_id
      description: The ID of the parent resource.
      required: true
      schema:
        type: integer
        example: 1
    objectiveSparseFieldsetParams:
      in: query
      name: fields[objectives]
      description: Requesting the API to return only the specified fields. For more information, see <a href="#section/Making-requests">Making requests</a>
      required: false
      schema:
        type: string
        example: title,description,reference,division_department,owner,executive_owner,created_at,updated_at,project,assigned_user,owner_user,executive_owner_user,custom_attributes,position,risk_control_matrix_id,walkthrough_summary_id,testing_round_1_id,testing_round_2_id,testing_round_3_id,testing_round_4_id,entities,framework,framework_origin,risk_assurance_data,planned_start_date,actual_start_date,planned_end_date,actual_end_date,planned_milestone_date,actual_milestone_date
  securitySchemes:
    bearerToken:
      description: 'HighBond offers the industry-standard OAuth 2.0 flow for connecting to the HighBond platform. OAuth 2.0 provides a

        safe and secure way to access data, while protecting your account credentials.

        '
      type: http
      scheme: bearer
      bearerFormat: oauth2
x-tagGroups:
- name: Core resources
  tags:
  - Robots Agents
  - Robots
  - Robot Tasks
  - Robot Jobs
  - Robot Apps
  - Robot Script Versions
  - Robot Activations
  - Robot Files
  - Robot Working Files
  - Robot Collaborators
  - Robots Folders Collaborators
  - Robot Users
  - Robots Folders
  - Storyboards
  - Collections
  - Questionnaires
  - Analyses
  - Surveys
  - Event Reports
  - Tables
  - Table columns
  - Records
  - Record columns
  - Record statuses
  - Interpretations
  - Metrics
  - Workflow Groups
  - Results Users
  - Results Triggers
  - Project types
  - Custom attributes
  - Request item statuses
  - Projects
  - Projects Admin
  - Planning files
  - Results files
  - Objectives
  - Narratives
  - Risks
  - Controls
  - Control performance schedules
  - Mitigations
  - Control test plans
  - Control tests
  - Attachments
  - Walkthroughs
  - Issues
  - Actions
  - Collaborators
  - Request Items
  - To-dos
  - Sign-offs
  - Entities
  - Scheduler Filters
  - Frameworks
  - Handlers
  - Workflows in Asset Inventory/Asset Manager
  - Asset types
  - Asset record types
  - Attribute types
  - Assets
  - Asset Relationships
  - Asset records
  - Asset Record Relationships
  - Events
  - Template Toolkits
  - Toolkits
  - Roles
  - Role Deletion
  - Compliance Maps
  - Time entries
  - Non-project time categories
  - Timesheets
  - Importer
  - System Users
  - Organizations
  - Impact Reports
  - Activities
  - Users
  - Groups
  - Scheduled Users
  - Scheduled Projects
  - Scheduled Hours
  - Extract
  - Strategy
